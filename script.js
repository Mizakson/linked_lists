// linked list project

class LinkedList {
    // if head is not initialized, head = null
    constructor(head = null) {
        this.head = head
    }
}


class Node {
    constructor(value) {
        this.value = null
        this.nextNode = null
    }
}